import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hack-a-tech-prizey',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
